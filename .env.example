# TopicMind Environment Variables
# -------------------------------
# Copy this file to .env and fill in your values
# This file is OPTIONAL. The application will work without it, but some advanced features may be limited.

# OpenAI API Key (Optional)
# Required only for GPT-based topic refinement
# If not provided, the application will use default topic names based on keywords
OPENAI_API_KEY=your_openai_api_key_here

# Model Configuration
# ------------------
# Device to use for model inference: 'cpu' or 'cuda' (GPU)
# Default: 'cpu'
MODEL_DEVICE=cpu

# Use smaller models to reduce memory usage (true/false)
# Default: false
USE_SMALL_MODELS=false

# System Requirements
# ------------------
# - At least 1GB of free disk space
# - Minimum 4GB RAM (8GB recommended)
# - Python 3.9 or higher

# Advanced Configuration
# ---------------------
# Maximum timeout for API requests (in seconds)
# Default: 60
API_TIMEOUT=60

# TopicMind Environment Variables
# Copy this file to .env and fill in your values

# Optional: OpenAI API Key (required only for GPT-based topic refinement)
# If not provided, the application will use default topic names based on keywords
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Model configuration options
# MODEL_DEVICE=cuda  # Uncomment for GPU acceleration if available
# USE_SMALL_MODELS=true  # Uncomment to use smaller models for resource-constrained environments
